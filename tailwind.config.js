/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // 匹配所有 HTML 和 TypeScript 文件
  ],
  theme: {
    extend: {}, // 在這裡擴展自定義設置（可選）
  },
  plugins: [],
};
